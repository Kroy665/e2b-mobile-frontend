# Override the backend base URL. If unset, the app infers it from the Expo dev
# server host on port 2345 (works for iOS simulator, Android emulator, and
# physical devices on the same network as the Metro bundler).
# EXPO_PUBLIC_API_URL=http://localhost:2345
